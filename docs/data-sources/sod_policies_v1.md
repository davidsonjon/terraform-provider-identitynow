---
page_title: "identitynow_sod_policies_v1 Data Source - identitynow"
subcategory: "Separation of Duties (SOD) Policies"
description: |-
  Lists Separation of Duties (SOD) Policies https://documentation.sailpoint.com/saas/help/sod/manage-policies.html from IdentityNow/ISC via GET /sod-policies/v1, optionally filtered/sorted/paginated.
  ~> This is a _v1 pilot data source. Per a live API check documented in the resource's "Known Limitations & Live Testing Notes", invoking this data source with a fully-known filters value performs a live API call at plan time, not just apply time.
---

# identitynow_sod_policies_v1 (Data Source)

Lists [Separation of Duties (SOD) Policies](https://documentation.sailpoint.com/saas/help/sod/manage-policies.html) from IdentityNow/ISC via `GET /sod-policies/v1`, optionally filtered/sorted/paginated.

~> This is a `_v1` pilot data source. Per a live API check documented in the resource's "Known Limitations & Live Testing Notes", invoking this data source with a fully-known `filters` value performs a live API call at `plan` time, not just `apply` time.

## Example Usage

```terraform
# List SOD policies, optionally filtered/sorted server-side via the
# standard V3 collection parameters syntax:
# https://developer.sailpoint.com/idn/api/standard-collection-parameters
data "identitynow_sod_policies_v1" "example" {
  filters = "state eq \"ENFORCED\""
  sorters = "name"
  limit   = 50
}

output "sod_policy_ids_by_name" {
  value = { for p in data.identitynow_sod_policies_v1.example.sod_policies : p.name => p.id }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (String) Filter results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results)

Filtering is supported for the following fields and operators:

**id**: *eq, in*

**name**: *eq, in*

**state**: *eq, in*
- `limit` (Number) Max number of results to return.
See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information.
- `offset` (Number) Offset into the full result set. Usually specified with *limit* to paginate through the results.
See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters) for more information.
- `sorters` (String) Sort results using the standard syntax described in [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#sorting-results)

Sorting is supported for the following fields: **id, name, created, modified, description**

### Read-Only

- `sod_policies` (Attributes Set) (see [below for nested schema](#nestedatt--sod_policies))

<a id="nestedatt--sod_policies"></a>
### Nested Schema for `sod_policies`

Read-Only:

- `compensating_controls` (String) Optional compensating controls(Mitigating Controls)
- `conflicting_access_criteria` (Attributes) The two access-item lists (left_criteria/right_criteria) that define a CONFLICTING_ACCESS_BASED policy - required (and only meaningful) when type = "CONFLICTING_ACCESS_BASED"; left as null for a type = "GENERAL" policy. Each list holds 1-50 ENTITLEMENT references; the API derives policy_query itself from this criteria. (see [below for nested schema](#nestedatt--sod_policies--conflicting_access_criteria))
- `correction_advice` (String) Optional correction advice
- `created` (String) The time when this SOD policy is created.
- `creator_id` (String) Policy's creator ID
- `description` (String) Optional description of the SOD policy
- `external_policy_reference` (String) Optional External Policy Reference
- `id` (String) Policy id
- `modified` (String) The time when this SOD policy is modified.
- `modifier_id` (String) Policy's modifier ID
- `name` (String) Policy Business Name
- `owner_ref` (Attributes) The owner of the SOD policy. (see [below for nested schema](#nestedatt--sod_policies--owner_ref))
- `policy_query` (String) Search query of the SOD policy
- `scheduled` (Boolean) defines whether a policy has been scheduled or not
- `state` (String) whether the policy is enforced or not
- `tags` (List of String) tags for this policy object
- `type` (String) whether a policy is query based or conflicting access based
- `violation_owner_assignment_config` (Attributes) Configures who is assigned as the owner of violations this policy generates. assignment_rule = "MANAGER" assigns the violating identity's manager; assignment_rule = "STATIC" assigns a specific owner_ref (an IDENTITY or GOVERNANCE_GROUP). (see [below for nested schema](#nestedatt--sod_policies--violation_owner_assignment_config))

<a id="nestedatt--sod_policies--conflicting_access_criteria"></a>
### Nested Schema for `sod_policies.conflicting_access_criteria`

Read-Only:

- `left_criteria` (Attributes) (see [below for nested schema](#nestedatt--sod_policies--conflicting_access_criteria--left_criteria))
- `right_criteria` (Attributes) (see [below for nested schema](#nestedatt--sod_policies--conflicting_access_criteria--right_criteria))

<a id="nestedatt--sod_policies--conflicting_access_criteria--left_criteria"></a>
### Nested Schema for `sod_policies.conflicting_access_criteria.left_criteria`

Read-Only:

- `criteria_list` (Attributes List) List of criteria (1-50 items). Each item references an ENTITLEMENT by id. (see [below for nested schema](#nestedatt--sod_policies--conflicting_access_criteria--left_criteria--criteria_list))
- `name` (String) Business name for this access construct list.

<a id="nestedatt--sod_policies--conflicting_access_criteria--left_criteria--criteria_list"></a>
### Nested Schema for `sod_policies.conflicting_access_criteria.left_criteria.criteria_list`

Read-Only:

- `id` (String) ID of the referenced object.
- `name` (String) Human-readable display name of the referenced object.
- `type` (String) Type of the referenced object. Only `ENTITLEMENT` is currently supported.



<a id="nestedatt--sod_policies--conflicting_access_criteria--right_criteria"></a>
### Nested Schema for `sod_policies.conflicting_access_criteria.right_criteria`

Read-Only:

- `criteria_list` (Attributes List) List of criteria (1-50 items). Each item references an ENTITLEMENT by id. (see [below for nested schema](#nestedatt--sod_policies--conflicting_access_criteria--right_criteria--criteria_list))
- `name` (String) Business name for this access construct list.

<a id="nestedatt--sod_policies--conflicting_access_criteria--right_criteria--criteria_list"></a>
### Nested Schema for `sod_policies.conflicting_access_criteria.right_criteria.criteria_list`

Read-Only:

- `id` (String) ID of the referenced object.
- `name` (String) Human-readable display name of the referenced object.
- `type` (String) Type of the referenced object. Only `ENTITLEMENT` is currently supported.




<a id="nestedatt--sod_policies--owner_ref"></a>
### Nested Schema for `sod_policies.owner_ref`

Read-Only:

- `id` (String) Owner's ID.
- `name` (String) Owner's name.
- `type` (String) Owner type.


<a id="nestedatt--sod_policies--violation_owner_assignment_config"></a>
### Nested Schema for `sod_policies.violation_owner_assignment_config`

Read-Only:

- `assignment_rule` (String) `MANAGER` or `STATIC`.
- `owner_ref` (Attributes) The static violation owner (set when `assignment_rule = "STATIC"`). (see [below for nested schema](#nestedatt--sod_policies--violation_owner_assignment_config--owner_ref))

<a id="nestedatt--sod_policies--violation_owner_assignment_config--owner_ref"></a>
### Nested Schema for `sod_policies.violation_owner_assignment_config.owner_ref`

Read-Only:

- `id` (String) Owner's ID.
- `name` (String) Owner's name.
- `type` (String) Owner type: `IDENTITY`, `GOVERNANCE_GROUP`, or `MANAGER`.

## Known Limitations & Live Testing Notes

Each entry in `sod_policies` is populated by the same conversion code as
the singular [`identitynow_sod_policy_v1` data source](../data-sources/sod_policy_v1.md) -
see its "Known Limitations & Live Testing Notes" section for the full list
of limitations shared by both.

`GET /sod-policies/v1` supports server-side `filters` and `sorters` query
parameters (the standard [V3 API collection parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters)
syntax), unlike `identitynow_segments_v1`'s equivalent list endpoint. The
API's own `count` query parameter (an `X-Total-Count` header toggle) is
intentionally dropped from this schema entirely - `count` is a reserved
root attribute/block name in Terraform, matching
`identitynow_governance_groups_v1`/`identitynow_segments_v1`'s equivalent
precedent.

**Be aware this data source performs a live API call at `terraform plan`
time whenever its arguments are fully known** (i.e. not derived from
another resource's unknown output) - harmless in normal use, but means a
`main.tf` exercising this data source is not appropriate if you're trying
to keep a `plan` check fully credential-free.

---
page_title: "identitynow_identity_profiles_v1 Data Source - identitynow"
subcategory: "Identity Profiles"
description: |-
  Lists Identity Profiles https://documentation.sailpoint.com/saas/help/setup/identity_profiles.html from IdentityNow/ISC via GET /identity-profiles/v1, optionally filtered, sorted, and paginated. Returns the same attributes per profile as the singular identitynow_identity_profile_v1 data source.
  ~> This is a _v1 pilot data source - see identitynow_identity_profile_v1's (the resource) "Known Limitations & Live Testing Notes" section before relying on it in production configurations; the same limitations apply to each profile returned here.
---

# identitynow_identity_profiles_v1 (Data Source)

Lists [Identity Profiles](https://documentation.sailpoint.com/saas/help/setup/identity_profiles.html) from IdentityNow/ISC via `GET /identity-profiles/v1`, optionally filtered, sorted, and paginated. Returns the same attributes per profile as the singular `identitynow_identity_profile_v1` data source.

~> This is a `_v1` pilot data source - see `identitynow_identity_profile_v1`'s (the resource) "Known Limitations & Live Testing Notes" section before relying on it in production configurations; the same limitations apply to each profile returned here.

## Example Usage

```terraform
# Lists identity profiles whose name starts with "Test", capped at 10
# results. Each entry in `identity_profiles` has the same attributes as
# identitynow_identity_profile_v1.
data "identitynow_identity_profiles_v1" "example" {
  filters = "name sw \"Test\""
  limit   = 10
}

output "test_identity_profile_ids" {
  value = [for p in data.identitynow_identity_profiles_v1.example.identity_profiles : p.id]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (String) Filter expression used to query identity profiles (e.g. `name eq "Employees"`). Filtering is supported for `id`, `name` (eq, ne, ge, gt, in, le, sw), and `priority` (eq, ne). See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#filtering-results).
- `limit` (Number) Maximum number of identity profiles to return. The API's documented maximum for this endpoint is 250; values above 250 are capped to 250 with a warning.
- `offset` (Number) Offset into the full result set, usually used with `limit` to paginate.
- `sorters` (String) Sort expression for the results. Sorting is supported for id, name, priority, created, modified, owner.id, owner.name. See [V3 API Standard Collection Parameters](https://developer.sailpoint.com/idn/api/standard-collection-parameters#sorting-results).

### Read-Only

- `identity_profiles` (Attributes List) Identity Profiles matching the query, each with the same attributes as `identitynow_identity_profile_v1`. (see [below for nested schema](#nestedatt--identity_profiles))

<a id="nestedatt--identity_profiles"></a>
### Nested Schema for `identity_profiles`

Read-Only:

- `authoritative_source` (Attributes) (see [below for nested schema](#nestedatt--identity_profiles--authoritative_source))
- `created` (String) Creation date of the Object
- `description` (String) Identity profile's description.
- `has_time_based_attr` (Boolean) Indicates the value of `requiresPeriodicRefresh` attribute for the identity profile.
- `id` (String) System-generated unique ID of the Object
- `identity_attribute_config` (String) The identity attribute mapping/transform configuration for this Identity Profile (`{enabled, attributeTransforms: [...]}`). Represented as a raw JSON object via `jsontypes.Normalized` rather than a fixed schema, since tfplugingen-openapi/-framework cannot generate a faithful static schema for a transform-type-discriminated union.
- `identity_count` (Number) Number of identities belonging to the identity profile.
- `identity_exception_report_reference` (Attributes) (see [below for nested schema](#nestedatt--identity_profiles--identity_exception_report_reference))
- `identity_refresh_required` (Boolean) Set this value to 'True' if an identity refresh is necessary. You would typically want to trigger an identity refresh when a change has been made on the source.
- `modified` (String) Last modification date of the Object
- `name` (String) Name of the Object
- `owner` (Attributes) Identity profile's owner. (see [below for nested schema](#nestedatt--identity_profiles--owner))
- `priority` (Number) Identity profile's priority.

<a id="nestedatt--identity_profiles--authoritative_source"></a>
### Nested Schema for `identity_profiles.authoritative_source`

Read-Only:

- `id` (String) Authoritative source's ID.
- `name` (String) Authoritative source's name.
- `type` (String) Authoritative source's object type.


<a id="nestedatt--identity_profiles--identity_exception_report_reference"></a>
### Nested Schema for `identity_profiles.identity_exception_report_reference`

Read-Only:

- `report_name` (String) Report name.
- `task_result_id` (String) Task result ID.


<a id="nestedatt--identity_profiles--owner"></a>
### Nested Schema for `identity_profiles.owner`

Read-Only:

- `id` (String) Owner's ID.
- `name` (String) Owner's name.
- `type` (String) Owner's object type.

## Known Limitations & Live Testing Notes

Each entry in `identity_profiles` is populated by the same conversion code as
the singular [`identitynow_identity_profile_v1` data source](../data-sources/identity_profile_v1.md) -
see its "Known Limitations & Live Testing Notes" section for the full list
of limitations shared by both.

`GET /identity-profiles/v1`'s documented maximum `limit` is 250; requested
limits above that are capped with a warning rather than an error.
